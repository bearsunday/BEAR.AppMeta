build:
    image: default-jammy
    environment:
        php: 8.4
    nodes:
        analysis:
            project_setup:
                override: true
            dependencies:
                before:
                    - apt-get update -qq
                    - apt-get install -y libssl-dev pkg-config
            tests:
                override:
                    - php-scrutinizer-run

filter:
    paths: ["src/*"]
